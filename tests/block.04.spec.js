var chai = require('chai');
var expect = chai.expect;

describe('rest and spread: ', function () {
    'use strict';   

    beforeEach(function () {
    });

    it('V8 doesn not support rest and spread yet', function(){      
       /* var getProduct = function(productId = 100){
            return { productId: productId };
        }
        expect(getProduct().productId).to.be.equal(100);*/
    });   

    describe('Rest', function(){  
       /* it('Unexpected token ...', function(){      
            var showCategories = function(productId, ...categories){
                return categories instanceof Array;
            };

            expect(showCategories('123','search','advertising')).to.be.true;
        });  
        */ 
    });  
});
